{% load_yaml as rawmap %}
Debian:
  tools:
    bzip:
      pkgs:
        - bzip2
    rar:
      pkgs:
        - rar
        - unrar
        - unrar-free
RedHat:
  tools:
    bzip:
      pkgs:
        - bzip2
    rar:
      pkgs: [] {# TODO need extra repo #}
    sqlite3:
      pkgs:
        - sqlite
{% endload %}

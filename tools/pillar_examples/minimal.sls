tools:
  lookup:
    tools:
      bzip:
        ensure: installed
      colordiff:
        ensure: installed
      curl:
        ensure: installed
      ed:
        ensure: installed
      gzip:
        ensure: installed
      msgpack_python:
        ensure: installed
        pkgs:
          - msgpack-python
